load("@rules_cc//cc:defs.bzl", "cc_library")
load("//build_defs:cpp_opts.bzl", "COPTS")

cc_library(
    name = "reaction",
    hdrs = glob(["reaction/**/*.h"]),
    copts = COPTS,
    strip_include_prefix = "/reaction",
    visibility = ["//reaction:__subpackages__"],
    deps = [
        "//common:noncopyable",
        "//common:singleton",
    ],
)
