load("@rules_cc//cc:defs.bzl", "cc_library")
load("//build_defs:cpp_opts.bzl", "COPTS")

cc_library(
    name = "reaction",
    copts = COPTS,
    features = ["cpp_modules"],
    module_interfaces = glob(["src/*.cppm"]),
    visibility = ["//reaction:__subpackages__"],
    deps = [
        "//common:noncopyable",
        "//common:singleton",
    ],
)
