load("@rules_qt//:qt.bzl", "qt_cc_binary")
load("//build_defs:cpp_opts.bzl", "COPTS")

qt_cc_binary(
    name = "99table",
    srcs = ["main.cpp"],
    copts = COPTS,
    features = ["-layering_check"],
    deps = [
        "@rules_qt//:qt_widgets",
    ],
)
