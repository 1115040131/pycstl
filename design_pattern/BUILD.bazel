load("@rules_cc//cc:defs.bzl", "cc_test")
load("//build_defs:cpp_opts.bzl", "COPTS")

cc_test(
    name = "design_pattern_test",
    srcs = glob(["*_test.cpp"]),
    copts = COPTS + ["-Wno-non-virtual-dtor"],
    features = ["cpp_modules"],
    module_interfaces = glob(["*.cppm"]),
    deps = [
        "//common:cin_redirect",
        "@glm",
        "@googletest//:gtest",
        "@googletest//:gtest_main",
    ],
)
