load("@rules_cc//cc:defs.bzl", "cc_binary")
load("//build_defs:cpp_opts.bzl", "COPTS")

cc_binary(
    name = "hello_world",
    srcs = [
        "debug.hpp",
        "hello_world.cpp",
    ],
    copts = COPTS,
)
