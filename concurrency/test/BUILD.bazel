load("@rules_cc//cc:defs.bzl", "cc_test")
load("//build_defs:cpp_opts.bzl", "COPTS")

cc_test(
    name = "concurrency_all_test",
    srcs = glob(
        [
            "*_test.cpp",
            "*.h",
        ],
        allow_empty = True,
    ),
    copts = COPTS + ["-Wno-thread-safety"],
    features = ["cpp_modules"],
    module_interfaces = glob(["*.cppm"]),
    deps = [
        "//common:noncopyable",
        "//concurrency",
        "@fmt",
        "@googletest//:gtest",
        "@googletest//:gtest_main",
    ],
)
