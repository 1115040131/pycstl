load("//build_defs:cpp_opts.bzl", "COPTS")

cc_test(
    name = "concurrency_all_test",
    srcs = glob([
        "*_test.cpp",
        "*.h",
    ]),
    copts = COPTS,
    includes = ["../"],
    deps = [
        "//concurrency",
        "@googletest//:gtest_main",
    ],
)
