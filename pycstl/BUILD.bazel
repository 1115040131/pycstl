load("@rules_cc//cc:defs.bzl", "cc_library")
load("//build_defs:cpp_opts.bzl", "COPTS")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "pycstl",
    hdrs = glob(["include/pycstl/**/*.h"]),
    copts = COPTS,
    includes = ["include"],
    deps = [
        "//common",
        "@fmt",
    ],
)
