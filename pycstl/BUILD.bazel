load("@rules_cc//cc:defs.bzl", "cc_library")
load("//build_defs:cpp_opts.bzl", "COPTS")

cc_library(
    name = "pycstl",
    copts = COPTS,
    features = ["cpp_modules"],
    module_interfaces = glob(["pycstl/*.cppm"]) + ["pycstl.cppm"],
    visibility = ["//visibility:public"],
    deps = ["//common:noncopyable"],
)
