load("//build_defs:cpp_opts.bzl", "COPTS")

cc_test(
    name = "pycstl_all_test",
    srcs = glob([
        "*_test.cpp",
        "*.h",
    ]),
    copts = COPTS,
    includes = ["../"],
    deps = [
        "//pycstl",
        "@googletest//:gtest_main",
    ],
)
