load("//build_defs:cpp_opts.bzl", "STRICT_COPTS")

package(default_visibility = ["//visibility:public"])

licenses(["notice"])

exports_files(["LICENSE"])

cc_library(
    name = "gate_server_lib",
    srcs = glob(["src/**/*.cpp"]),
    hdrs = glob(["include/**/*.h"]),
    copts = STRICT_COPTS,
    data = glob(["config/*"]),
    includes = ["include"],
    deps = [
        "//chat/server/gate_server/proto:verify_cc_grpc_proto",
        "//common",
        "@boost//:asio",
        "@boost//:beast",
        "@boost//:property_tree",
        "@com_github_grpc_grpc//:grpc++",
        "@fmt",
        "@nlohmann_json//:json",
    ],
)

cc_binary(
    name = "gate_server",
    srcs = ["main.cpp"],
    copts = STRICT_COPTS,
    deps = [
        ":gate_server_lib",
    ],
)
