load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library")
load("//build_defs:cpp_opts.bzl", "COPTS")

cc_library(
    name = "chat_server_lib",
    srcs = glob(["src/**/*.cpp"]),
    hdrs = glob(["include/**/*.h"]),
    copts = COPTS,
    strip_include_prefix = "include",
    deps = [
        "//chat/common",
        "//chat/server/common",
        "//chat/server/proto:chat_cc_grpc_proto",
        "//chat/server/proto:chat_cc_proto",
        "//common:connection_pool",
        "//common:noncopyable",
        "//common:singleton",
        "//common:utils",
        "//logger",
        "@boost.asio",
        "@fmt",
        "@grpc//:grpc++",
        "@nlohmann_json//:json",
    ],
)

cc_binary(
    name = "chat_server",
    srcs = ["chat_server.cpp"],
    copts = COPTS,
    visibility = ["//chat/server:__subpackages__"],
    deps = [
        ":chat_server_lib",
        "//chat/server/common",
        "//common:connection_pool",
        "//common:noncopyable",
        "//common:singleton",
        "//common:utils",
        "@grpc//:grpc++",
    ],
)
