load("//build_defs:cpp_opts.bzl", "COPTS")

cc_test(
    name = "coroutine_all_test",
    srcs = glob([
        "*_test.cpp",
        "*.h",
    ]),
    copts = COPTS,
    includes = ["../"],
    deps = [
        "//coroutine",
        "@googletest//:gtest_main",
    ],
)
