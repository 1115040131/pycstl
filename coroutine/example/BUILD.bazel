load("//build_defs:cpp_opts.bzl", "COPTS")

BINARIES = [
    "exception_handle",
    "variant_result",
    "template_task",
    "sleep_for"
]

[cc_binary(
    name = binary,
    srcs = ["%s.cpp" % binary],
    copts = COPTS,
    deps = ["//coroutine"],
) for binary in BINARIES]
