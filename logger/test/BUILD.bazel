load("//build_defs:cpp_opts.bzl", "COPTS")

cc_test(
    name = "logger_all_test",
    srcs = glob([
        "*_test.cpp",
        "*.h",
    ]),
    copts = COPTS,
    includes = ["../"],
    deps = [
        "//logger",
        "@googletest//:gtest_main",
    ],
)
