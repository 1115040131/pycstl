load("//build_defs:cpp_opts.bzl", "STRICT_COPTS")

cc_test(
    name = "logger_all_test",
    srcs = glob([
        "*_test.cpp",
        "*.h",
    ]),
    copts = STRICT_COPTS,
    includes = ["../"],
    deps = [
        "//logger",
        "@googletest//:gtest_main",
    ],
)
