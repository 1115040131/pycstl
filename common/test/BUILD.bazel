load("//build_defs:cpp_opts.bzl", "COPTS")

cc_test(
    name = "common_all_test",
    srcs = glob([
        "*_test.cpp",
        "*.h",
    ]),
    copts = COPTS,
    includes = ["../"],
    deps = [
        "//common",
        "@googletest//:gtest_main",
    ],
)
