load("//build_defs:cpp_opts.bzl", "STRICT_COPTS")

cc_binary(
    name = "gol",
    srcs = ["gol.cpp"],
    copts = STRICT_COPTS,
    data = glob(["resource/**"]),
    deps = [
        "//tetris",
        "@fmt",
    ],
)
