package(default_visibility = ["//visibility:public"])

licenses(["notice"])

exports_files(["LICENSE"])

cc_library(
    name = "network",
    srcs = glob(["src/*.cpp"]),
    hdrs = glob(["include/network/*.h"]),
    includes = [
        ".",
        "include",
    ],
    deps = [
        "@boost//:asio",
        "@fmt",
    ],
)
